using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UseItem : Move
{
    // Start is called before the first frame update
    public override void ability(Character user, Character target)
    {
        //user.;
    }
}
